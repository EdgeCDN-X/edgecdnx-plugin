worker_processes auto;
error_log /dev/stderr info;
pid /tmp/nginx.pid;

events { worker_connections 4096; }

http {
  access_log /dev/stdout combined;
  log_format edge_metrics '$remote_addr - $remote_user [$time_local] "$request" $status $body_bytes_sent $request_length $request_time $upstream_response_time $upstream_cache_status';
  access_log /var/log/nginx/edge-access.log edge_metrics;
  proxy_temp_path /tmp/proxy_temp;
  proxy_cache_path /var/cache/nginx levels=1:2 keys_zone=hls_cache:32m max_size=256m inactive=10m use_temp_path=off;

  server {
    listen 8080;

    location ~ \.mp4$ {
      proxy_pass http://127.0.0.1:8666;
      proxy_http_version 1.1;
      proxy_set_header Host $host;
      proxy_cache hls_cache;
      proxy_cache_key "$scheme$request_method$host$uri";
      proxy_cache_valid 200 10m;
      proxy_cache_lock on;
      proxy_cache_use_stale error timeout updating http_500 http_502 http_503 http_504;
      proxy_ignore_headers Cache-Control Expires Set-Cookie;
      proxy_hide_header Set-Cookie;
      add_header X-Cache-Status $upstream_cache_status always;
    }

    location ~ \.m3u8$ {
      proxy_pass http://127.0.0.1:8666;
      proxy_http_version 1.1;
      proxy_set_header Host $host;
      proxy_cache hls_cache;
      proxy_cache_key "$scheme$request_method$host$uri";
      proxy_cache_valid 200 1s;
      proxy_cache_lock on;
      proxy_ignore_headers Cache-Control Expires Set-Cookie;
      proxy_hide_header Set-Cookie;
      add_header X-Cache-Status $upstream_cache_status always;
    }

    location / {
      proxy_pass http://127.0.0.1:8666;
      proxy_http_version 1.1;
      proxy_set_header Host $host;
      add_header X-Cache-Status BYPASS always;
    }

    location = /healthz {
      access_log off;
      return 200 'ok\n';
    }

    location = /stub_status {
      access_log off;
      stub_status;
      allow 127.0.0.1;
      deny all;
    }
  }
}
