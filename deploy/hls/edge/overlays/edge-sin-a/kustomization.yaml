apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namePrefix: edge-sin-a-
resources: [../../base]
configMapGenerator:
  - name: edge-config
    behavior: merge
    files: [toxiproxy.json]
labels:
  - pairs: {edgeroute.io/node: edge-sin-a, edgeroute.io/location: singapore, edgeroute.io/service: hls-demo}
    includeSelectors: true
  - pairs: {node: edge-sin-a, location: singapore, service: hls-demo}
patches:
  - target: {kind: Deployment, name: edge}
    patch: |-
      - op: add
        path: /spec/template/spec/nodeSelector
        value: {edge-role: singapore}
  - target: {kind: Service, name: edge}
    patch: |-
      - op: add
        path: /spec/clusterIP
        value: 10.96.10.13
